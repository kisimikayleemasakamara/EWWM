@model IEnumerable<WasteMangement.Models.publicWasteBin>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Dashboard.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.publicWasteBinName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.publicWasteBinDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.isDeleted)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.collectionSite.collectionSiteManager)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.publicWasteBinName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.publicWasteBinDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.isDeleted)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.collectionSite.collectionSiteManager)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.publicWasteBinId }) |
            @Html.ActionLink("Details", "Details", new { id=item.publicWasteBinId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.publicWasteBinId })
        </td>
    </tr>
}

</table>
